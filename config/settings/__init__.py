"""
Настройки проекта разбиты на три файла:

  base.py — общий фундамент (приложения, БД, безопасность, шаблоны)
  dev.py  — локальная разработка (DEBUG=True, SQLite, подробные ошибки)
  prod.py — продакшен (DEBUG=False, PostgreSQL, сжатая статика)

Какой файл использовать — решается переменной окружения DJANGO_SETTINGS_MODULE
(управляющий скрипт manage.py по умолчанию берёт dev.py).
"""
