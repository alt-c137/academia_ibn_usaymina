# Основные зависимости платформы.
# Версии зафиксированы: платформа должна подниматься на тех же версиях, что и тестировалась.
Django>=5.2,<5.3        # 5.2 — LTS-релиз, поддержка до апреля 2028
pillow>=10.4,<12        # обработка изображений (обложки курсов, новости)
python-dotenv>=1.0,<2   # чтение настроек из файла .env
whitenoise>=6.7,<7      # раздача статики без отдельного сервера
